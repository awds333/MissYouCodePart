package com.fedchanka.missyou.service.navigation

import androidx.fragment.app.Fragment

class CreateDialogNavigationAction(private val parent: Fragment,
                                   private val fragmentProvider: FragmentProvider) : NavigationAction {
    override fun navigate() {
        
    }
}